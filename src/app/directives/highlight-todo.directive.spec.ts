import { HighlightTodoDirective } from './highlight-todo.directive';

describe('HighlightTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTodoDirective();
    expect(directive).toBeTruthy();
  });
});
